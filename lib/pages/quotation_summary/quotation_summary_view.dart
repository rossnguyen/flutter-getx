
import 'package:flutter/material.dart';

class QuotationSummaryView extends StatelessWidget {
  const QuotationSummaryView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: const Text('Quotation summary')),
        body: Center(
      child: Text('Quotation Sunnart View'),
    ));
  }
}
